# Generated GitHub Actions workflow for AI Insights Blog - Complete Production Deployment
# This file was automatically generated by the CI/CD Pipeline Generator
# Modify with caution - regenerate when application structure changes

name: Docker CI Pipeline
'on':
  push:
    branches:
    - main
    - develop
  pull_request:
    branches:
    - main
jobs:
  docker-build-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    - name: Setup Docker Buildx
      uses: docker/setup-buildx-action@v3
    - name: Build Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        load: true
        tags: test-image:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    - name: Test Docker image
      run: docker run --rm test-image:latest --version || docker run --rm test-image:latest --help
    - name: Run security scan on image
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: test-image:latest
        format: sarif
        output: trivy-results.sarif
    - name: Upload security scan results
      uses: github/codeql-action/upload-sarif@v3
      with:
        sarif_file: trivy-results.sarif
